\documentclass[letterpaper, 10pt]{article}

% Geometry package for page margins.
\usepackage[
left=0.70in,
right=0.70in,
top=0.7in,
bottom=0.40in,
nohead]{geometry}

%% Font settings.
% Needed to update Tex Live from 2025 to 2026
% $ wget http://mirror.ctan.org/systems/texlive/tlnet/update-tlmgr-latest.sh
% $ sudo sh update-tlmgr-latest.sh 
% $ sudo tlmgr install tinos

\usepackage{fontspec}
% \setmainfont{Arial}
\setmainfont{Open Sans}
% \setmainfont{Tinos}
% \setmainfont{Georgia}

